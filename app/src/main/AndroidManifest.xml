<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.griot_app.griot">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainOverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainProfileOverviewActivity"></activity>
        <activity android:name=".MainChooseFriendInputActivity"></activity>
        <activity android:name=".MainQuestionmailActivity"></activity>
        <activity android:name=".MainNotificationsActivity"></activity>
        <activity android:name=".MainTopicCatalogActivity"></activity>
    </application>

</manifest>