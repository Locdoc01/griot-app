<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.griot_app.griot">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".StartActivity"
            android:theme="@style/NoTitleBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:theme="@style/NoTitleBarTheme" >
        </activity>

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat">
        </activity>

        <activity
            android:name=".MainOverviewActivity">
        </activity>

        <activity
            android:name=".MainProfileOverviewActivity"
            android:parentActivityName=".MainOverviewActivity">
        </activity>

        <activity
            android:name=".MainChooseFriendInputActivity"
            android:parentActivityName=".MainOverviewActivity">
        </activity>

        <activity
            android:name=".MainQuestionmailActivity"
            android:parentActivityName=".MainOverviewActivity">
        </activity>

        <activity
            android:name=".MainNotificationsActivity"
            android:parentActivityName=".MainOverviewActivity">
        </activity>

        <activity
            android:name=".MainTopicCatalogActivity"
            android:parentActivityName=".MainOverviewActivity">
        </activity>

    </application>

</manifest>